/**
 * DVCS Platform — backend-webhook module.
 *
 * <p>Handles webhook registration, HMAC-SHA256 signed delivery,
 * exponential-backoff retry logic, and delivery history.
 */
package com.dvcs.webhook;
