/**
 * DVCS Platform — backend-git module.
 *
 * <p>Implements the Git object model (Blob, Tree, Commit), content-addressable
 * object storage (local filesystem and S3/MinIO backends), pack-file encoding
 * and decoding, and the HTTP Smart Git transport protocol (upload-pack / receive-pack).
 */
package com.dvcs.git;
