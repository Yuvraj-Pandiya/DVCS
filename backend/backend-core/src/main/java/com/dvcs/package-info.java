/**
 * DVCS Platform — backend-core module.
 *
 * <p>Contains core domain models, JPA repositories, security infrastructure
 * (JWT, rate limiting), caching, and shared utilities used by all other modules.
 */
package com.dvcs;
