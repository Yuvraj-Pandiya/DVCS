/**
 * DVCS Platform — backend-diff module.
 *
 * <p>Implements the Myers diff algorithm, three-way merge with conflict detection,
 * binary file detection, and patch application for the diff engine.
 */
package com.dvcs.diff;
