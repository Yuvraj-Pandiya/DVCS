/**
 * DVCS Platform — backend-pr module.
 *
 * <p>Manages the pull request lifecycle: creation, review submission,
 * inline comments, and merge strategies (merge commit, squash, rebase).
 */
package com.dvcs.pullrequest;
